# 线上家装设计 AI Agent —— 项目总结（AI PM 求职版）

## 一、一句话定位

**「线上家装设计 AI Agent」**：一个把「毛坯房照片 → AI 效果图 → 设计方案 → 报价 → 服务商撮合 → 下单履约」全链路搬到线上的 AI 家装产品，从 PRD 到可上线部署的完整产品，**一人端到端完成产品定义、AI 方案设计与全栈研发**（2026-07 立项，08 上线 MVP）。

---

## 二、项目背景与痛点

传统家装链路（设计、施工、采购）强依赖线下：决策周期长（数天到数周）、信息不透明、报价无标准、履约难监管。本项目用 AI 预方案作为高频入口，帮助业主**分钟级**拿到装修方向，再用透明报价、服务商撮合、采购链接和订单履约完成交易闭环。

**业务目标**：
- 户型图 → 初步装修方案，从数天缩短到分钟级
- 提升方案留资、预约顾问、签约转化率
- 建立标准化报价、节点验收与服务商评价体系，降低纠纷率

---

## 三、产品设计（PM 视角）

### 3.1 用户角色与核心诉求

| 角色 | 核心诉求 |
| --- | --- |
| 业主 | 快速看到靠谱方案、预算清晰、施工放心 |
| 设计团队 | 获取高意向线索，降低前期沟通成本 |
| 施工团队 | 获取标准项目、减少扯皮 |
| 家具/建材/家电商 | 精准导购与采购订单 |
| 平台运营 | 撮合交易、监管履约、处理争议 |
| **AI Agent** | 理解空间与需求，生成可落地建议 |

### 3.2 核心业务闭环（7 步流程）

上传建档 → 需求澄清（AI 追问）→ AI 预方案生成（≥3 套）→ 比选修改 → 报价清单（BOM）→ 撮合签约 → 深化履约

### 3.3 MVP 范围

1. 户型图上传识别（JPG/PNG/PDF，不确定区域标记待确认）
2. 需求问卷 + AI 追问，输出强约束/偏好/可妥协项
3. 生成 ≥3 套预方案（布局、风格、预算区间、材料家具建议）
4. 方案对比与局部重生成、预算档位切换
5. 留资与预约顾问线索转化

### 3.4 成功指标（PRD 定义）

| 指标 | 目标 |
| --- | --- |
| 方案生成完成率 | MVP ≥ 80% |
| 方案收藏/修改率 | ≥ 35% |
| 留资/预约率 | ≥ 10% |
| 签约转化率 | 试点期 ≥ 15% |
| 施工节点准时率 | ≥ 85% |
| NPS | ≥ 40 |

---

## 四、AI 能力设计（面试重点）

### 4.1 多模态生图链路：毛坯照片 → 效果图

- **图生图**：以上传的毛坯照片为参考图（已做 EXIF 摆正、缩放、亮度/对比度/锐化增强），保持原户型、门窗、视角不变，只替换饰面、家具与灯光 —— 解决「同一套房装完什么样」的核心场景
- **文生图**：不参考照片，按风格+需求出图，用于早期风格探索
- **多服务商适配层**（`imagegen.py`）：统一收敛 maizi / openai / pollinations 三种图像服务商差异；**智能降级**：服务商不支持参考图或调用失败时自动退回文生图，并在前端明确提示
- 效果图后处理：调色、缩放、水印，保证交付质量

### 4.2 Prompt 工程化：控制模块体系

- 提示词不硬编码，拆成 **22 个「Prompt 控制模块」**（7 大维度：灯光氛围/材质质感/镜头视角/色彩基调/布局收纳/情绪风格/画质控制）
- **前端只提交模块 code，提示词文本仅存后端** —— 避免 prompt 暴露给客户端、未知 code 直接忽略不污染
- 模块可配置权重、默认组合、按空间/风格限定适用范围，运营可在后台直接调优提示词片段
- 输入约束（图片格式/大小/尺寸、需求字数、敏感信息过滤、模块数量）**前后端同一口径双重校验**

### 4.3 可编排生图工作流引擎（核心架构亮点）

生图不是写死的代码，而是**后台可编辑的流水线**（`RenderWorkflow` + `WorkflowStep`，16 种步骤类型）：

- 上传图预处理（校验/摆正/缩放/增强）→ 画面分析（亮度/色调/构图）→ 家具库匹配 → 提示词装配 → 生图 → 成图后处理（调色/缩放/水印）→ 交付（设计说明/匹配施工队设计师）
- **按标签自动匹配工作流**：根据空间/风格/预算档位自动选择最合适的流水线（如「客厅 + 现代简约 + 品质 → 高保真出片流程」）
- 预置 4 条流程：标准交付（13 步）/ 快速预览（8 步）/ 高保真出片（13 步）/ 灵感文生图（7 步）
- **容错设计**：默认单步失败不阻塞整单，错误写入 workflow_log，用户仍能拿到图；可切换严格终止模式
- 每次生成保留完整执行轨迹（步骤状态、耗时、摘要），后台可逐段排查

### 4.4 智能化匹配与文本生成

- 家具/施工队/设计师按「空间 + 风格 + 城市 + 预算」智能匹配（如避免客厅出现床的错配）
- AI 设计说明：走文本大模型生成装修说明与搭配理由，失败时降级规则文案
- 图像分析（亮度/色调/构图）结果可注入 prompt，让模型理解原始空间状态
- 门洞标记（OpenCV 图像处理）：识别门洞区域并在 prompt 中禁止摆放家具，提升方案可落地性

### 4.5 多窗口并行工作台

- 一个页面同时开 6 个任务窗口并行生成（3 个并发 + 自动排队），支持复制参数、逐窗对比
- 窗口状态机：draft → validating → queued → running → success/failed
- 「发散灵感」：一次返回多套 prompt 模块组合方案，可一键套用或**每个方案各开一个窗口并行生成对比**

---

## 五、架构与技术栈

```
前端 Vue3 + Vite + pinia + vue-router（SPA）
        │  REST API
后端 Django 5 + DRF（业务/工作流引擎/生图适配/权限）
        │
生图服务（maizi / openai / pollinations 多 Provider 适配 + 降级）
图像处理（OpenCV / Pillow / pillow-heif）
数据库（本地 SQLite WAL → 生产 PostgreSQL）
部署（Docker Compose：nginx + gunicorn + PostgreSQL + 命名卷）
CI/CD（GitHub Actions：自动构建检查 + SSH 自动发布）
```

**关键工程实践**：
- 单端口部署（Django 托管 Vite 产物），本地一键脚本 `serve.sh`（构建→collectstatic→migrate→起服务）
- 超时链路设计：nginx 600s > gunicorn 600s > 前端 360s > 后端轮询 300s
- SQLite WAL + 写操作退避重试，解决多窗口并发 `database is locked`
- 全配置环境变量化（django-environ），API Key 走后台配置不入代码
- 输入安全：需求文案过滤手机号/邮箱/身份证/链接/HTML 字符

---

## 六、我的角色与交付

**角色：产品负责人 + AI 方案设计 + 全栈研发（一人端到端）**

- **产品侧**：撰写完整 PRD V1.0（背景目标、用户角色、MVP 范围、核心流程、功能需求、成功指标、非功能约束）
- **AI 侧**：设计生图链路、Prompt 控制模块体系、可编排工作流引擎、多 Provider 适配与降级策略
- **工程侧**：Django 后端（约 4800 行）+ Vue 前端（约 2700 行）+ 15 张数据模型 + 20+ REST API + 部署与 CI
- **交付物**：可运行的 MVP 全栈系统（约 1 个月：2026-07 PRD → 08 上线），含 15 条业务数据模型（业主/项目/方案/线索/服务商/家具/渲染任务/报告/订单等）、18 个前后端功能页面、Docker 生产部署方案

---

## 七、简历与面试可用亮点

1. **AI 产品闭环能力**：不是单一"调 API"，而是把生图、prompt 工程、工作流编排、智能匹配、容错降级串成完整产品链路
2. **Prompt 工程化思维**：控制模块化、前后端分离 prompt 权限、按空间/风格动态组装、可运营调优
3. **抽象与扩展性设计**：16 种步骤的可编排工作流引擎 + 标签自动匹配，运营不改代码即可调整产品流程 —— 体现"用平台化思维做 AI 产品"
4. **用户体验细节**：多窗口并行对比、发散灵感、图生图/文生图模式透明提示、降级时明确告知用户
5. **工程落地能力**：一人覆盖前后端、多 Provider 适配、性能超时设计、生产部署，PM 懂技术能极大降低与研发的沟通成本
6. **商业化思维**：全链路到订单履约，设计了对平台有真实商业价值的闭环（留资→签约→采购）

## 八、简历一句话版本

> 从 0 到 1 打造「线上家装设计 AI Agent」：独立完成 PRD、AI 方案设计与全栈研发，实现毛坯照片→AI 效果图→设计方案→报价→撮合→下单的全链路闭环；核心亮点为可编排生图工作流引擎（16 类步骤、按标签自动匹配、失败容错降级）与 Prompt 控制模块体系（22 模块、7 维度、前后端分离），支持图生图/文生图多服务商适配与智能降级；已上线可部署 MVP。

